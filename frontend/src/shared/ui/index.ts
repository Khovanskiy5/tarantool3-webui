/**
 * Shared UI kit — entry point.
 *
 * Domain-free primitives wrapped over PrimeVue: buttons, inputs, tables,
 * banners, toast helpers, layout containers. New primitives land here
 * as their owning tasks are implemented; this index then re-exports
 * them so every consumer imports through `@/shared/ui`.
 *
 * Hard rule: no entity / feature / page imports may appear in any
 * descendant of this folder. The ESLint boundaries plugin enforces this.
 */

export {};
